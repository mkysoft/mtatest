ID: mtatest
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: restdemo
   type: java
   path: restdemo
   parameters:
      memory: 512M
   provides:
    - name: restdemo_api
      properties:
         url: ${default-url}
